# Daily-library validation

The first repeatable real connection chain is complete. Its second normal browser
download, cross-run SHA-256 comparison, immutable-version reuse and restarted
cache-only query were accepted before this follow-up. M1–M9 are retained.

## Current collection boundary

A later bounded browser visit discovered six accessible courses. Four formal
courses were prioritized; the other two remain in the private selection inventory
with explicit pending scopes. Normal visible download controls and the installed
Core/CLI captured 66 originals: 62 PDF, three DOCX and one unsupported PPTX. One
previously accepted original was reused; it was not downloaded again. Text parsing
produced results for 65 supported files. These counts do not imply complete text
or event extraction.

Course metadata, available folder/file observations, announcements and supported
assessment overviews entered the existing provider. Non-file document bodies,
folder descriptions, embedded attachments, unavailable submissions and external
interactive modules remain separately accounted for. No quiz, submission, roster,
completion control, external video transcription or additional account was used.

An embedded document's normal download control led to a Chrome
`ERR_BLOCKED_BY_CLIENT` page. No HTTP status was observed. That path was stopped;
other ordinary outline-file downloads remained usable. No terminal, credential or
alternate-network workaround was attempted. The component enforcing that isolated
restriction is not established by the available evidence.

Detailed course identities, captures, per-course counts, timestamps, pending items,
manual labels and evaluation output stay in the private runtime. A corrected host
translation retains its original capture timestamp; importing it is not a new
remote observation.

## Interpretation

- File discovery, original storage, native parsing, indexing and visual review are
  distinct coverage layers. Bounded UI traversal is not proof of remote completeness.
- Local recent-material results describe recorded observations. Initial library
  collection is not evidence that every collected file was recently uploaded.
- Requirements can be retrieved from source text even when they have no structured
  event. Empty event results do not establish that no arrangement exists.
- The original frozen evaluation has eight source-local event mentions. A separate
  reviewer selected and labeled another document before examining system results;
  its selected pages contain 16 mentions and nine requirement groups. Neither
  sample estimates recall across all courses.
- Known past dates, week-only statements, unassigned presentation slots, ambiguous
  dates and contradictory source instructions require explicit treatment in answers.

The supported trial lane is a documented browser-host-assisted collection followed
by local Core/CLI/Skill queries. The separate raw API interface, automatic SSO
renewal and unsupported formats are not prerequisites for that bounded lane.
Publication remains a separate human decision; no remote or package publication
has been performed.

## Processing and evidence preservation

An updated native parser contract produced new reusable parse records for the same
66 immutable originals. No new binary version was needed. Two diagnostically flagged
PDF pages were rendered with local Poppler, visually inspected using the authorized
host, and imported through the installed CLI. Their supplemental text retains source
hash, physical page, render hash, engine/settings identity, confidence, uncertainty
and PARTIAL review status. Native text and originals remain immutable. A third
inspected page had usable native text and was not forced into the visual workflow.

Repeating the two identical visual imports preserved all resource-version, parse,
representation, extraction, candidate, event, source and Claim counts. This is a
derived-cache check, not another live connection verification. Earlier failed local
jobs are retained in history; successful new processing does not silently clear them.

The current diagnostic inventory flags 58 chunks for potential visual review. Two
have bounded supplemental descriptions and 56 remain uninspected. Even the inspected
pages retain PARTIAL status. This count is not an estimate of missed events.

A later current-UI title check used a new bounded capture and no download. Its
limited content observation did not advance original-file freshness. Regression
review found that UNKNOWN resource observations from an incomplete visit could
look like availability changes; the recent-material query now excludes UNKNOWN
from comparison history while retaining those records for source inspection.
Explicit observed unavailability remains reportable.

## Bounded extraction evaluation

The frozen original sample now yields **8/8 mentions**, no extra event mentions, **26/27
expected fields** and **27/27 verified provenance paths**. The three previously missed
image-only arrangements are represented, and the previously generic title is specific.
The remaining strict mismatch is a broader submission type versus the gold assignment-due
type; its title and date/time agree. The negative document sample still has no candidates.
This is mention-level acceptance, not proof of complete canonical-event reconciliation.

An independent reviewer read the entire supplemental document before inspecting output
and froze selected-page labels: 16 mentions and nine requirement groups. Re-evaluation
finds **13/16 mentions**, no extra event mentions and **39/39 emitted field provenance paths**.
Only 11 mentions have canonical events; two weak identities remain unresolved. None of
the 16 mentions has every frozen field correct: omissions, incomplete titles, uncertain
week precision and five wrong event classifications remain. Of 112 gold field units,
25 are correct, 18 correctly abstain, 64 are missing, three titles are partial and two
week precisions remain partial/UNKNOWN. All nine requirement groups are retrievable
from sources, but none is a structured requirement record. One gold wording exceeds
its source; the reviewer recorded that label defect without silently changing the gold.

These results support source-backed retrieval and experimental event previews. They do
not support an authoritative calendar or a claim that all course arrangements are found.
Native text, visual evidence and earlier extraction histories remain available for audit.

## Normal-use acceptance and final checks

A fresh agent session read the installed local question Skill and answered five Chinese
questions through the installed CLI: upcoming work, assessment requirements, arrangements
in slides, recent material observations and a cache-only exact source. Bilingual query
decomposition and bounded source fallbacks found English evidence. The final session
made 47 CLI calls, all reporting `refresh_attempted=false`; it used no browser, network,
internal database inspection, rendering or import. Partial envelopes were retained.
The host Skill's conversational routing is assisted lexical retrieval, not a standalone
natural-language CLI or general semantic-search guarantee.

Source comparisons exposed a deadline difference between an assessment and its attachment,
and an older template date conflicting with a current briefing. Answers disclosed these
instead of relying on an empty structured-conflict list. Past or uncertain event candidates
and scientific uses of the word “test” still require source checks. Recent-material answers
report recorded observations, not teacher upload dates or a replayed capture as a refresh.

Final integrated verification on Python 3.12: **556 tests passed**, Ruff lint/format,
strict mypy (61 source files), compile and wheel/sdist build passed. Independent reviews
accepted the implementation and repair slices. This follow-up did not repeat the earlier
Phase 5 multi-version matrix or dependency advisory audit. The final repeated normal CLI
sync and two visual imports preserved all eight checked derived-identity counts; their
partial exit codes contained no errors. SQLite integrity was `ok`, with no foreign-key
violations. Local hash/size checks covered all 66 originals; they do not verify remote bytes.

Assessment: **local daily retrieval trial supported; limited experimental retrieval
candidate with documented constraints**. Event extraction remains a preview requiring
manual source review. Unsupported formats, unfinished bodies and visual review remain
resumable private work. No public destination, push or publication is part of acceptance.

Current candidate privacy checks passed for public files, wheel/sdist contents and Git
history, including private-identity canaries. A clean offline wheel installation passed
console help, cache-only empty-store and safe error checks. Relative documentation links
passed. These are current local-candidate checks, not a future publication authorization.
